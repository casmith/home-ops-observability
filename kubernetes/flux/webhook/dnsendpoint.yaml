---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: flux-webhook-pihole
  namespace: flux-system
  labels:
    external-dns.io/provider: pihole
spec:
  endpoints:
    - dnsName: flux-webhook-obs.kalde.in
      recordTTL: 300
      recordType: CNAME
      targets:
        - external-obs.kalde.in
